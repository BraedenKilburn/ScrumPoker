services:
  frontend:
    build:
      context: ./frontend
      target: build
    env_file:
      - ./frontend/.env.production
    depends_on:
      - backend
    volumes:
      - frontend_build:/app

  backend:
    build:
      context: ./backend
      target: release
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  frontend_build:
